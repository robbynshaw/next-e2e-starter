const routes = {
  index: "/",
  auth: {
    login: "/auth",
    seed: {
      confirm: "/auth/seed/confirm",
      import: "/auth/seed/import",
      prep: "/auth/seed/prep"
    }
  }
}

export default routes;