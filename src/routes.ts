const routes = {
  index: "/",
  auth: {
    login: "/auth",
    seed: {
      confirm: "/auth/seed/confirm",
      import: "/auth/seed/import",
      prep: "/auth/seed/prep",
      generate: "/auth/seed/generate"
    }
  }
};

export default routes;
